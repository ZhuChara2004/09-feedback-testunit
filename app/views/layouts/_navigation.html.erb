<nav class="navbar navbar-default navbar-static-top" role="navigation" style="margin-bottom: 0">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to 'Homework 09', root_path, class: 'navbar-brand' %>
  </div>

  <ul class="nav navbar-top-links navbar-right">
    <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="#">
        <i class="fa fa-user fa-fw"></i>  <i class="fa fa-caret-down"></i>
      </a>
      <ul class="dropdown-menu dropdown-user">
        <% if user_signed_in? %>
          <li>
            <%= link_to destroy_user_session_path, method: "delete"  do %>
              <i class="fa fa-sign-out"></i> Log out
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to new_user_session_path do %>
            <i class="fa fa-sign-in fa-fw"></i> Sign in
          <% end %>
          </li>
          <li>
            <%= link_to new_user_registration_path do %>
              <i class="fa fa-users fa-fw"></i> Sign up
            <% end %>
          </li>
        <% end %>
      </ul>
    </li>
  </ul>

  <div class="navbar-default sidebar" role="navigation">
    <div class="sidebar-nav navbar-collapse">
      <ul class="nav in" id="side-menu">
        <li>
          <a href="#"><%= user_signed_in? ? current_user.email : 'Log in first' %></a>
        </li>
        <li>
          <% if user_signed_in? %>
            <%= link_to feedbacks_path do %>
              <i class="fa fa-table fa-fw"></i> List Feedbacks
            <% end %>
          <% else %>
            <%= link_to new_feedback_path do %>
              <i class="fa fa-edit fa-fw"></i> Create feedback
            <% end %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>